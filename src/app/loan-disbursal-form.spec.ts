import { LoanDisbursalForm } from './loan-disbursal-form';

describe('LoanDisbursalForm', () => {
  it('should create an instance', () => {
    expect(new LoanDisbursalForm()).toBeTruthy();
  });
});
